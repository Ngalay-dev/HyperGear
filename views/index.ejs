<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>HyperGear</title>
</head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">HyperGear</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
          <ul class="navbar-nav">
            <% if (!user) {%>
                <li class="nav-item">
                    <a class="nav-link" href="/register">Register</a>
                </li> 
                <li class="nav-item">
                    <a class="nav-link" href="/Login">Login</a>
                </li> 
            <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/Logout">Logout</a>
                </li> 
            <% } %>
          </ul>
<form action="/search" method="GET">
  <input type="text" name="Search item" placeholder="Search item" required>
  <button type="submit">Search</button>
</form>

<ul>
  <% if (results && results.length > 0) { %>
    <% results.forEach(item => { %>
      <li><%= products.productName %> - <%= products.price %></li>
    <% }) %>
  <% } else { %>
    <li>No results found</li>
  <% } %>
</ul>
        </div>
      </div>
    </nav>

    <div class="container">
        <p style="text-align: center;">
            <% if (user) {%>
              <% if (user.role === "admin") { %>
                <br>
                Back to <a href="/inventory">inventory!</a>
              <% } else { %>
                <br>
                Continue <a href="/shopping">Shopping!</a>
                <% } %>
            <% } else { %>
            <br>
            Welcome to HyperGear! <br>
            Please <a href = "/login" >login</a> to continue...
            <% } %>
        </p>
        
    </div>
</body>
</html>
